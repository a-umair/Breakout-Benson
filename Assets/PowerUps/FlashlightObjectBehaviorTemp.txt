using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlashlightObjectBehavior : MonoBehaviour {
    public static bool flashlightAbility = false;
    // Use this for initialization
    void Start () {
		
	}
   
    bool OnTriggerEnter (Collider other)
    {
        flashlightAbility = true;
        Destroy(gameObject);
        return flashlightAbility;
    }
    // Update is called once per frame
    void Update () {
		
	}
}